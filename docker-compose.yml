version: '3'

services:
  backend:
    build:
      context: ./server
      dockerfile: ./Dockerfile
    image: "venus/quantum"
    ports:
      - "8000:8000"
  frontend:
    build:
      context: ./
      dockerfile: ./Dockerfile
    image: "venus/theswitch"
    network_mode: "host"
    environment:
      VEGA_WEB_SERVER_URL: "https://seng426-team3-vega-web-backend.azurewebsites.net"


